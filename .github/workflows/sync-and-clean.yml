name: Sync Printful to Stripe & Clean Broken Mappings

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 6 AM UTC
  workflow_dispatch:

jobs:
  sync-and-clean:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run sync script (Printful → Stripe)
        env:
          MODE: live
          DRY_RUN: false
          STRIPE_SECRET_TEST: ${{ secrets.STRIPE_SECRET_TEST }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          PRINTFUL_API_KEY: ${{ secrets.PRINTFUL_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node sync-printful-to-stripe.js

      - name: Run clean broken mappings (fix metadata / deactivate bad prices)
        env:
          DRY_RUN: false
          PRINTFUL_API_KEY: ${{ secrets.PRINTFUL_API_KEY }}
          STRIPE_SECRET_TEST: ${{ secrets.STRIPE_SECRET_TEST }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
        run: node clean-broken-mappings.js

      - name: Remove duplicate products (by name) from TEST and LIVE
        env:
          DRY_RUN: false
          STRIPE_SECRET_TEST: ${{ secrets.STRIPE_SECRET_TEST }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
        run: node clean-duplicate-stripe-products.js

      - name: Show logs on failure
        if: failure()
        run: echo "❌ Workflow failed. Check logs above for details."